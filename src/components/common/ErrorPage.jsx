import React from 'react';
import Alert from '@mui/material/Alert';

const ErrorPage = () => (
  <Alert severity="error">No Podemos Encontrar la página solicitada :(</Alert>

);

export default ErrorPage;
